{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCommand": "php artisan storage:link && chmod -R 755 storage/app/public public/storage && php artisan serve --port=${PORT}",
      "webCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}"
    }
  },
  "deploy": {
    "startCommand": "bash -c 'php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT}'"
  }
}