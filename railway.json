{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCommand": "php artisan serve --host=0.0.0.0 --port=8000",
      "webCommand": "php artisan serve --host=0.0.0.0 --port=8000"
    }
  },
  "deploy": {
    "startCommand": "bash -c 'php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=8000'"
  }
}